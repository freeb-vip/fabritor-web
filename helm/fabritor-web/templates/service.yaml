apiVersion: v1
kind: Service
metadata:
  name: {{ include "fabritor-web.fullname" . }}
  labels:
    {{- include "fabritor-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fabritor-web.selectorLabels" . | nindent 4 }}
